import React from 'react'
import { Typography } from '@mui/material'

function Title14600(props) {
    const {text} = props
    return (
      <Typography sx={{ fontSize: 14, fontWeight: 600, color: '#333333' }}>{text}</Typography>
    )
}

export default Title14600